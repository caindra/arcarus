{% extends 'layout.html.twig' %}

    {% block title %}Admin Panel{% endblock %}

    {% block content %}
<div class="columns is-align-items-center head">
    <div class="column is-narrow">
        <img src="{{ asset('images/logos/arcarus_purple.png') }}" alt="logo" class="image is-64x64">
    </div>
    <div class="column">
        <h1 class="title">General</h1>
    </div>
</div>

        <div class="buttons">
            <a href="{{ path('groups') }}" class="button is-link is is-large">Groups</a>
            <a href="{{ path('academic-years') }}" class="button is-link is-large">Academic Years</a>
            <a href="{{ path('organizations') }}" class="button is-link is-large">Organizations</a>
        </div>
    {% endblock %}
